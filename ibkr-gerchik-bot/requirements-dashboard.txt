# Dashboard-only dependencies (kept separate from the bot runtime).
# Install into the same venv:  pip install -r requirements-dashboard.txt
streamlit>=1.40,<2
plotly>=5.24,<7
pandas>=2.0,<3
openpyxl>=3.1,<4
